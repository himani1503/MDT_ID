<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MedTrack - Universal Medical Tracking System</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .page {
            display: none;
            background: white;
            border-radius: 15px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            overflow: hidden;
            width: 100%;
            min-height: 500px;
        }

        .page.active {
            display: block !important;
            visibility: visible !important;
        }

        /* Header Styles */
        .header {
            background: linear-gradient(135deg, #2c3e50, #34495e);
            color: white;
            padding: 30px;
            text-align: center;
        }

        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }

        .header p {
            font-size: 1.2em;
            opacity: 0.9;
        }

        /* Home Page Styles */
        .hero-section {
            padding: 50px 30px;
            text-align: center;
        }

        .hero-section h2 {
            color: #2c3e50;
            font-size: 2.2em;
            margin-bottom: 20px;
        }

        .hero-section p {
            color: #666;
            font-size: 1.1em;
            line-height: 1.6;
            margin-bottom: 30px;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            padding: 0 30px 50px;
        }

        .feature-card {
            background: #f8f9fa;
            padding: 30px;
            border-radius: 10px;
            text-align: center;
            transition: transform 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-5px);
        }

        .feature-card .icon {
            font-size: 3em;
            margin-bottom: 15px;
        }

        .btn {
            background: linear-gradient(135deg, #3498db, #2980b9);
            color: white;
            padding: 15px 30px;
            border: none;
            border-radius: 25px;
            font-size: 1.1em;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin: 10px;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(52, 152, 219, 0.3);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #95a5a6, #7f8c8d);
        }

        /* Login Page Styles */
        .login-container {
            padding: 50px 30px;
            max-width: 500px;
            margin: 0 auto;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #2c3e50;
            font-weight: 500;
        }

        .form-group input, .form-group select {
            width: 100%;
            padding: 15px;
            border: 2px solid #ecf0f1;
            border-radius: 8px;
            font-size: 1em;
            transition: border-color 0.3s ease;
        }

        .form-group input:focus, .form-group select:focus {
            outline: none;
            border-color: #3498db;
        }

        .form-tabs {
            display: flex;
            margin-bottom: 30px;
            background: #ecf0f1;
            border-radius: 8px;
        }

        .tab-btn {
            flex: 1;
            padding: 15px;
            background: none;
            border: none;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 1em;
        }

        .tab-btn.active {
            background: #3498db;
            color: white;
            border-radius: 8px;
        }

        .form-section {
            display: none;
        }

        .form-section.active {
            display: block;
        }

        /* Dashboard Styles */
        .dashboard {
            padding: 30px;
            position: relative;
        }

        .logout-btn {
            position: absolute;
            top: 20px;
            right: 30px;
            z-index: 10;
        }

        .dashboard-header {
            display: flex;
            justify-content: flex-start;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid #ecf0f1;
            padding-top: 60px; /* Add space for logout button */
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .avatar {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, #3498db, #2980b9);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5em;
            font-weight: bold;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
            padding: 20px;
            border-radius: 10px;
            text-align: center;
        }

        .stat-number {
            font-size: 2em;
            font-weight: bold;
            color: #2c3e50;
        }

        .stat-label {
            color: #666;
            margin-top: 5px;
        }

        .history-section {
            background: #f8f9fa;
            padding: 25px;
            border-radius: 10px;
            margin-top: 20px;
        }

        .history-item {
            background: white;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 10px;
            border-left: 4px solid #3498db;
        }

        .prescription-tag {
            background: #e74c3c;
            color: white;
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 0.8em;
            margin-left: 10px;
        }

        .otc-tag {
            background: #27ae60;
            color: white;
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 0.8em;
            margin-left: 10px;
        }

        .alert {
            background: #ffe6e6;
            border: 1px solid #ffcccc;
            color: #d63031;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        @media (max-width: 768px) {
            .container {
                padding: 10px;
            }
            
            .header h1 {
                font-size: 2em;
            }
            
            .features {
                grid-template-columns: 1fr;
                padding: 0 15px 30px;
            }
            
            .dashboard-header {
                flex-direction: column;
                gap: 15px;
                text-align: center;
                padding-top: 40px; /* Reduce space on mobile */
            }
            
            .logout-btn {
                position: static;
                width: 100%;
                margin-bottom: 20px;
            }
            
            .dashboard .btn[onclick="showPage('homePage')"] {
                position: static !important;
                width: 100%;
                margin-bottom: 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Home Page -->
        <div id="homePage" class="page active">
            <div class="header">
                <h1>🏥 MedTrack</h1>
                <p>Universal Medical ID System</p>
            </div>
            
            <div class="hero-section">
                <h2>MedTrack ID</h2>
                <p>Prevent drug abuse and reduce crimes through secure KYC verification and comprehensive medicine tracking. One ID, complete medical history, safer communities.</p>
                
                <button class="btn" onclick="showPage('loginPage')">Get Started</button>
                <button class="btn btn-secondary" onclick="scrollToFeatures()">Learn More</button>
            </div>
            
            <div class="features" id="features">
                <div class="feature-card">
                    <div class="icon">🆔</div>
                    <h3>Universal ID System</h3>
                    <p>Every individual gets a unique, KYC-verified ID that tracks all medical purchases across different stores and healthcare providers.</p>
                </div>
                
                <div class="feature-card">
                    <div class="icon">🔍</div>
                    <h3>Real-time Monitoring</h3>
                    <p>Track suspicious patterns in narcotic purchases and prevent potential misuse before it leads to criminal activities.</p>
                </div>
                
                <div class="feature-card">
                    <div class="icon">📊</div>
                    <h3>Complete History</h3>
                    <p>Permanent, tamper-proof records of all medical purchases help healthcare providers make informed decisions.</p>
                </div>
                
                <div class="feature-card">
                    <div class="icon">🛡️</div>
                    <h3>Crime Prevention</h3>
                    <p>Reduce drug-related crimes, illegal sales, and prevent medication-assisted suicides through systematic tracking.</p>
                </div>
                
                <div class="feature-card">
                    <div class="icon">👥</div>
                    <h3>Healthcare Integration</h3>
                    <p>Seamless integration with doctors, pharmacies, and medical stores for comprehensive healthcare management.</p>
                </div>
                
                <div class="feature-card">
                    <div class="icon">🔐</div>
                    <h3>Secure & Private</h3>
                    <p>Advanced encryption and privacy protection ensure your medical information remains confidential yet trackable for safety.</p>
                </div>
            </div>
        </div>

        <!-- Login Page -->
        <div id="loginPage" class="page">
            <div class="header">
                <h1>🏥 MedTrack Login</h1>
                <p>Access Your Medical History</p>
            </div>
            
            <div class="login-container">
                <div class="form-tabs">
                    <button class="tab-btn active" onclick="showTab('loginTab')">Login</button>
                    <button class="tab-btn" onclick="showTab('registerTab')">Generate ID</button>
                </div>
                
                <!-- Login Form -->
                <div id="loginTab" class="form-section active">
                    <form id="loginForm">
                        <div class="form-group">
                            <label for="loginId">MedTrack ID</label>
                            <input type="text" id="loginId" placeholder="Enter your MedTrack ID">
                        </div>
                        
                        <div class="form-group">
                            <label for="loginPassword">Password</label>
                            <input type="password" id="loginPassword" placeholder="Enter your password">
                        </div>
                        
                        <button type="button" class="btn" style="width: 100%;" onclick="goToDashboard()">Login</button>
                    </form>
                </div>
                
                <!-- Registration Form -->
                <div id="registerTab" class="form-section">
                    <form id="registerForm">
                        <div class="form-group">
                            <label for="fullName">Full Name</label>
                            <input type="text" id="fullName" placeholder="Enter your full name">
                        </div>
                        
                        <div class="form-group">
                            <label for="phoneNumber">Phone Number</label>
                            <input type="tel" id="phoneNumber" placeholder="Enter your phone number">
                        </div>
                        
                        <div class="form-group">
                            <label for="dob">Date of Birth</label>
                            <input type="date" id="dob">
                        </div>
                        
                        <div class="form-group">
                            <label for="email">Email Address</label>
                            <input type="email" id="email" placeholder="Enter your email address">
                        </div>
                        
                        <div class="form-group">
                            <label for="aadharKyc">Aadhar Number (KYC)</label>
                            <input type="text" id="aadharKyc" placeholder="Enter your 12-digit Aadhar number" maxlength="12">
                        </div>
                        
                        <div class="form-group">
                            <label for="registerPassword">Create Password</label>
                            <input type="password" id="registerPassword" placeholder="Create a strong password">
                        </div>
                        
                        <div class="form-group">
                            <label for="confirmPassword">Confirm Password</label>
                            <input type="password" id="confirmPassword" placeholder="Confirm your password">
                        </div>
                        
                        <button type="button" class="btn" style="width: 100%;" onclick="goToDashboard()">Generate MedTrack ID</button>
                    </form>
                </div>
                
                <button class="btn btn-secondary" onclick="showPage('homePage')" style="width: 100%; margin-top: 20px;">Back to Home</button>
            </div>
        </div>

        <!-- Dashboard Page -->
        <div id="dashboardPage" class="page">
            <div class="header">
                <h1>🏥 MedTrack Dashboard</h1>
                <p>Your Complete Medical History</p>
            </div>
            
            <div class="dashboard">
                <button class="btn btn-secondary logout-btn" onclick="logout()">Logout</button>
                <button class="btn" onclick="showPage('homePage')" style="position: absolute; top: 20px; left: 30px; z-index: 10;">Back</button>
                
                <div class="dashboard-header">
                    <div class="user-info">
                        <div class="avatar" id="userAvatar">JD</div>
                        <div>
                            <h3 id="userName">John Doe</h3>
                            <p>MedTrack ID: <strong id="userMedId">MT-2024-001234</strong></p>
                        </div>
                    </div>
                </div>
                
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-number" id="totalPurchases">47</div>
                        <div class="stat-label">Total Purchases</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number" id="prescriptionMeds">12</div>
                        <div class="stat-label">Prescription Medicines</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number" id="otcMeds">35</div>
                        <div class="stat-label">OTC Medicines</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-number" id="monthlyPurchases">8</div>
                        <div class="stat-label">This Month</div>
                    </div>
                </div>
                
                <div class="alert" id="alertSection" style="display: none;">
                    <strong>⚠️ Alert:</strong> <span id="alertMessage"></span>
                </div>
                
                <div class="history-section">
                    <h3>Recent Purchase History</h3>
                    <div id="purchaseHistory">
                        <!-- Purchase history will be loaded here -->
                    </div>
                    
                    <button class="btn" onclick="loadMoreHistory()" style="margin-top: 20px;">Load More History</button>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Sample data storage (in a real app, this would be a backend database)
        let users = {};
        let currentUser = null;
        let purchaseHistory = [
            {
                date: '2024-08-01',
                medicine: 'Paracetamol 500mg',
                type: 'OTC',
                quantity: '10 tablets',
                store: 'Apollo Pharmacy',
                doctor: null
            },
            {
                date: '2024-07-28',
                medicine: 'Amoxicillin 250mg',
                type: 'Prescription',
                quantity: '21 capsules',
                store: 'MedPlus',
                doctor: 'Dr. Smith'
            },
            {
                date: '2024-07-25',
                medicine: 'Cough Syrup',
                type: 'OTC',
                quantity: '100ml',
                store: 'Local Pharmacy',
                doctor: null
            },
            {
                date: '2024-07-20',
                medicine: 'Tramadol 50mg',
                type: 'Prescription',
                quantity: '10 tablets',
                store: 'Apollo Pharmacy',
                doctor: 'Dr. Johnson'
            },
            {
                date: '2024-07-15',
                medicine: 'Vitamin D3',
                type: 'OTC',
                quantity: '30 tablets',
                store: 'MedPlus',
                doctor: null
            }
        ];

        // Page navigation
        function showPage(pageId) {
            // Hide all pages
            document.querySelectorAll('.page').forEach(page => {
                page.classList.remove('active');
                page.style.display = 'none';
            });
            
            // Show selected page
            const targetPage = document.getElementById(pageId);
            if (targetPage) {
                targetPage.classList.add('active');
                targetPage.style.display = 'block';
            }
        }

        // Tab switching
        function showTab(tabId) {
            document.querySelectorAll('.tab-btn').forEach(btn => {
                btn.classList.remove('active');
            });
            document.querySelectorAll('.form-section').forEach(section => {
                section.classList.remove('active');
            });
            
            event.target.classList.add('active');
            document.getElementById(tabId).classList.add('active');
        }

        // Scroll to features
        function scrollToFeatures() {
            document.getElementById('features').scrollIntoView({ behavior: 'smooth' });
        }

        // Generate MedTrack ID
        function generateMedTrackId() {
            const year = new Date().getFullYear();
            const randomNum = Math.floor(100000 + Math.random() * 900000);
            return `MT-${year}-${randomNum}`;
        }

        // Simple function to go directly to dashboard
        function goToDashboard() {
            // Set default user
            currentUser = users['MT-2024-001234'];
            
            // Show dashboard page
            showPage('dashboardPage');
            
            // Load dashboard data
            loadDashboard();
        }

        // Load dashboard data
        function loadDashboard() {
            if (!currentUser) return;
            
            // Update user info
            document.getElementById('userName').textContent = currentUser.name;
            document.getElementById('userMedId').textContent = currentUser.id;
            document.getElementById('userAvatar').textContent = currentUser.name.split(' ').map(n => n[0]).join('').toUpperCase();
            
            // Load purchase history
            loadPurchaseHistory();
            
            // Check for alerts
            checkForAlerts();
        }

        // Load purchase history
        function loadPurchaseHistory() {
            const historyContainer = document.getElementById('purchaseHistory');
            historyContainer.innerHTML = '';
            
            purchaseHistory.forEach(purchase => {
                const historyItem = document.createElement('div');
                historyItem.className = 'history-item';
                
                const typeTag = purchase.type === 'Prescription' ? 
                    '<span class="prescription-tag">Prescription</span>' : 
                    '<span class="otc-tag">OTC</span>';
                
                const doctorInfo = purchase.doctor ? 
                    `<br><small>Prescribed by: ${purchase.doctor}</small>` : '';
                
                historyItem.innerHTML = `
                    <div style="display: flex; justify-content: space-between; align-items: center;">
                        <div>
                            <strong>${purchase.medicine}</strong> ${typeTag}
                            <br><small>Quantity: ${purchase.quantity} | Store: ${purchase.store}</small>
                            ${doctorInfo}
                        </div>
                        <div style="text-align: right;">
                            <small>${new Date(purchase.date).toLocaleDateString()}</small>
                        </div>
                    </div>
                `;
                
                historyContainer.appendChild(historyItem);
            });
        }

        // Check for alerts
        function checkForAlerts() {
            const prescriptionMeds = purchaseHistory.filter(p => p.type === 'Prescription').length;
            const recentNarcotics = purchaseHistory.filter(p => 
                p.medicine.toLowerCase().includes('tramadol') || 
                p.medicine.toLowerCase().includes('codeine') ||
                p.medicine.toLowerCase().includes('morphine')
            ).length;
            
            if (recentNarcotics > 0) {
                document.getElementById('alertSection').style.display = 'block';
                document.getElementById('alertMessage').textContent = 
                    'Narcotic purchases detected. This activity is being monitored for safety compliance.';
            }
        }

        // Load more history (placeholder)
        function loadMoreHistory() {
            alert('Loading older purchase history...\n(This would load more records from the database)');
        }

        // Logout
        function logout() {
            currentUser = null;
            showPage('homePage');
            
            // Clear forms
            document.getElementById('loginForm').reset();
            document.getElementById('registerForm').reset();
        }

        // Initialize with some sample data
        users['MT-2024-001234'] = {
            id: 'MT-2024-001234',
            name: 'Himani Hingne',
            phone: '9878913456',
            dob: '15.3.2004',
            email: 'himani@gmail.com',
            aadhar: '123456789012',
            password: 'demo123',
            createdDate: new Date().toISOString()
        };
    </script>
</body>

</html>
